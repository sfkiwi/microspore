config:
  target: "http://localhost:3000"
  phases:
    - duration: 60
      arrivalRate: 10
      name: "Initial"
scenarios:
  - name: "Query New Order Events"
    flow:
      - loop:
        - get:
            url: "/api/orders?year=2017&month=11&day=8"
        count: 50